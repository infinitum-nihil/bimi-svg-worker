{
  "name": "bimi-svg-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --experimental-strip-types test/run.ts",
    "check-drift": "node scripts/check-spec-drift.mjs",
    "build-check": "wrangler deploy --dry-run --outdir=dist"
  },
  "dependencies": {
    "linkedom": "^0.18.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.80.0"
  }
}
